/// <reference types="vite/client" />

/** Content stamp of the generated worklet parameter tables, substituted at bundle time by
    the euclidParams plugin (build/euclidParams.ts) so the engine can detect a stale
    engine-params.js served from an old cache. */
declare const __EUCLID_PARAMS_STAMP__: string;
